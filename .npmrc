# @langchain/community declares many OPTIONAL peer integrations (e.g. stagehand)
# with stale constraints (zod 3, dotenv 16) that we never import. Ignore peer
# resolution for those so the modern v1 stack (zod 4, elasticsearch 9) installs.
legacy-peer-deps=true
